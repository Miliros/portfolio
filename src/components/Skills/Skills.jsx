import React from "react";
import { Element } from "react-scroll";

export default function Skills() {
  return (
    <section
      id="skills"
      style={{
        height: "100vh",
        backgroundColor: "blue",
      }}
    >
      <p> Componente Skills</p>
    </section>
  );
}
